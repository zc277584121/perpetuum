[project]
name = "perpetuum-runtime"
version = "0.3.0"
description = "Local runner and frontend for the Perpetuum skill"
requires-python = ">=3.9"
dependencies = [
  "croniter>=2.0,<7",
  "PyYAML>=6.0,<7",
]

[tool.uv]
package = false
